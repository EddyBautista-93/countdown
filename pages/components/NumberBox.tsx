import React from "react";

interface numberProp {
    num: number | string,
    unit: number,
    flip: boolean
}

export const NumberBox = ({ num, unit, flip } :numberProp) => {
    return (
        
    )
}